
--- page 1 ---
U4-D1 Lesson – Radical Func5ons  If you hadn’t no-ced by now, radical is a general term for numerical roots (square roots, cube roots, etc.). This lesson is solely about square roots, but they usually get called radical func-ons.  Ex.1 Graph 𝑦=√𝑥 using the table. State domain and range.   
     Radical func-ons are wide, so it’s only prac-cal to use three points to graph. Note that this is analogous to the three sets of points (ﬁve total) we use for parabolas.  That last fact is no coincidence – quadra-c and radical func-ons are inverse func5ons, which is a can of worms for another day. The basic idea is that, because squares and square roots are “opposites,” their graphs have a par-cular rela-onship (ver-cal vs. horizontal, tall vs. wide, etc.).     The reason a radical func-on doesn’t have a “boLom half” is so that it s-ll passes the _____________________ test, without which it wouldn’t actually be a _______________.  From this point on, assessments may include a non-calculator sec5on. All of the graphing techniques we learn presume that we’re not using calculators, and certain exponent or logarithm expressions can’t be tested if you have access to them.   There’s only one form for radical func-ons:  𝑓(𝑥)=𝑎√𝑥−ℎ+𝑘  It doesn’t have a vertex, but it has a star-ng point that we ﬁnd the same way: (ℎ,𝑘)  From there, we have a couple of op-ons. The most reliable is probably seQng the inside of the radical (the argument, which is a general term for what’s “inside” of a func-on) equal to a pair of ﬁxed values; the least eﬀort is to use “the paLern.” 
𝑥 𝑦 −1  0  1  2  4   3 

--- page 2 ---
We’ll do Ex.2 both ways. AVer that I’ll focus on the paLern.  Ex.2 Graph using three points. State domain and range.   𝑦=√𝑥+3−2             When 𝑎≠1, the paLern gets a familiar adjustment: “up 𝑦" becomes “up 𝑎×𝑦.” If you’re not using the paLern, the 𝑎 gets included automa-cally as a mul-plier in the func-on.  Ex.3 Graph using three points. State domain and range.  a) 𝑦=−2√𝑥−1+5    b) 𝑦=!"√𝑥+3                     

--- page 3 ---
One huge diﬀerence between radicals and the func-ons we’ve seen previously (absolute value, quadra-c) has to do with their symmetry. It wouldn’t have maLered if we ﬂipped a V or U horizontally, so we didn’t even men-on it. But a radical func-on can point right or leV.  Be careful with the sign of 𝒉 when there’s a −𝒙! We usually “change the sign” of ℎ, due to the minus in (𝑥−ℎ). The extra nega-ve changes it back, so ℎ keeps its sign with a −𝑥.  When in doubt, you can always ﬁnd ℎ by seQng the argument equal to 0 and solving for 𝑥.   Ex.4 Graph using three points. State domain and range.   𝑦=√−𝑥  𝑥 𝑦 4  1  0  −1  −4       If you use the paLern, a minus on the x means you count leV instead of right. If you use the alterna-ve technique, the minus gets accounted for when you ﬁnd your x’s.  Ex.5 Graph using three points. State domain and range   𝑦=√2−𝑥+4              

--- page 4 ---
You may also be asked to ﬁnd domain and range of a radical func-on without graphing.   A formal way to iden-fy the domain is to set the argument greater than or equal to zero, which is the same as saying we can’t have nega-ve values inside the radical.   An alterna-ve is to iden-fy the star-ng point (“vertex”), which gives either the maximum or minimum 𝑥 and 𝑦, and then think about which direc-ons the graph is poin-ng.             Ex.6 Express the domain and range of each func-on using interval nota-on.  a) 𝑦=−6√𝑥+13−18    b) 𝑓(𝑥)=2√−𝑥−5+21               Ex.7 Describe the transforma-ons occurring in the func-on 𝑦=−𝑓(6−𝑥)+1. 